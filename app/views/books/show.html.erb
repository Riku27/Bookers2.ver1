<!DOCTYPE html>
<html class="fontawesome-i2svg-active fontawesome-i2svg-complete">
  <head>
    <title>Bookers2Ver2</title>
    <meta name="csrf-params" content="authenticity_token">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <style type="text/css"></style>
  </head>
  <main>
  <div>
  <%= flash[:notice] %>
  </div>
    <div class="container px-5 px-sm-0">
    <div class="row">
      <div class="col-md-3">
        <h2>User info</h2>
        <table class="table">
          <tbody>
            <tr><%= image_tag @book.user.get_profile_image(100,100) %></tr>
            <tr>
              <th>name</th>
              <th><%= @book.user.name %></th>
              <tr>
                <th>introduction</th>
                <th><%= @book.user.introduction %></th>
              </tr>
            </tr>
          </tbody>
        </table>
        <div class="row">
          <%= link_to edit_user_path(@book.user.id), class:"btn btn-outline-secondary btn-block" do %>
          <i class="fas fa-user-cog"></i>
          <% end %>
            <svg class="svg-inline--fa fa-user-cog fa-w-20" aria-hidden="true" focusable="false"
                    data-prefix="fas" data-icon="user-cog" role="img" xmlns="http://ww.w3.org/2000/svg"
                    viewBox="0 0 640 512" data-fa-i2svg></svg>
          </a>
        </div>
        <h2 class="mt-3">New book</h2>
        <%= form_with model: @books do |f| %>
            <h4>Title</h4>
            <%= f.text_field :title %>
            <h4>Opinion</h4>
            <%= f.text_area :body %>

            <%= f.submit 'Create Book',class: "btn btn-success" %>
       <% end %>

      </div>
      <div class="col-md-8 offset-md-1">
        <h2>Book detail</h2>
        <table class="table table-hover table-inverse">
          <thead>
          </thead>
          <tbody>

              <tr>
                <td>
                <%= image_tag @book.user.get_profile_image(80,80) %>
                <p><%= link_to @book.user.name, user_path(@book.user.id) %></p>
                </td>

                <td>
                 <%= link_to @book.title, book_path %>
                </td>
                <td>
                 <%= @book.body %>
                </td>
                <td>
                 <% if @book.user == current_user %>
                 <%= link_to "Edit", edit_book_path(@book.id),class: "btn btn-success" %>
                 <% end %>
                </td>
                <td>
                 <% if @book.user == current_user %>
                 <%= link_to "Destroy", book_path(@book.id), method: :delete, "data-confirm" => "本当に消しますか？",class: "btn btn-sm btn-danger" %>
                 <% end %>
                </td>
              </tr>

          </tbody>
        </table>
      </div>
    </div>
  </main>
</html>
