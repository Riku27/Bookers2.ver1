<!DOCTYPE html>
<html class="fontawesome-i2svg-active fontawesome-i2svg-complete">
  <head>
    <title>Bookers2Ver2</title>
    <meta name="csrf-params" content="authenticity_token">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <style type="text/css"></style>
  </head>
  <main>
    <div class="container px-5 px-sm-0">
    <div class="row">
      <div class="col-md-3">
        <h2>User info</h2>
        <table class="table">
          <tbody>
            <tr><%= image_tag current_user.get_profile_image(100,100) %></tr>
            <tr>
              <th>name</th>
              <th><%= current_user.name %></th>
              <tr>
                <th>introduction</th>
                <th></th>
              </tr>
            </tr>
          </tbody>
        </table>
        <div class="row">
          <%= link_to "btn btn-outline-secondary btn-block edit_user_id", edit_user_path(current_user.id) %>
            <svg class="svg-inline--fa fa-user-cog fa-w-20" aria-hidden="true" focusable="false"
                    data-prefix="fas" data-icon="user-cog" role="img" xmlns="http://ww.w3.org/2000/svg"
                    viewBox="0 0 640 512" data-fa-i2svg></svg>
          </a>
        </div>
        <h2 class="mt-3">New book</h2>
        <%= form_with model: @book do |f| %>
            <h4>Title</h4>
            <%= f.text_field :title %>
            <h4>Opinion</h4>
            <%= f.text_area :body %>

            <%= f.submit 'Create Book', class: "btn btn-success" %>
       <% end %>

      </div>
      <div class="col-md-8 offset-md-1">
        <h2>Books</h2>
        <table class="table table-hover table-inverse">
          <thead>
           <tr>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
            <th colspan="3"></th>
           </tr>
          </thead>
          <tbody>
            <% @books.each do |book| %>
              <tr>
                <td>
                <%= image_tag current_user.get_profile_image(80,80) %>
                </td>
                <td>
                <%= link_to book.title, book_path(book.id) %>
                </td>
                <td>
                <%= book.body %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </main>
</html>

